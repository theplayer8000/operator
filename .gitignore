node_modules
dist
.DS_Store
*.local

# Scratch / review artifacts — not part of the project record
onboarding-report.txt
*.scratch.md

# Local data store — the app writes here at runtime
data/
tsconfig.tsbuildinfo
